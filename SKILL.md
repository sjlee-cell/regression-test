---
name: test-automation-builder
description: 기획 산출물 기반 회귀 테스트 자동화 파이프라인. 케이스 리스트 작성 → 자동화 코드 변환 → 실행까지 단계별로 진행한다. 1·2차는 개발 전 문서 기반, 3차는 개발 완료 후 코드 기반.
allowed-tools: Read, Grep, Glob, Write, Edit, Bash, Agent
---

# 회귀 테스트 자동화 빌더

기획 산출물(IA, 기능명세서, 요구사항정의서, PRD 등)을 기반으로 회귀 테스트 케이스 리스트를 생성한다.
이 리스트는 결함 수정 후 배포 전에, 전체 기능에 문제가 없는지 확인하는 데 사용된다.

## 실행 방법

사용자가 프로젝트 폴더 경로를 제공하면 해당 폴더에서 기획 산출물을 찾아 읽는다.
경로를 제공하지 않은 경우, 현재 작업 디렉토리에서 기획 산출물을 탐색한다.

## 진행 순서

이 스킬은 **4단계로 나누어** 진행한다.

- **0차**: 분리 축(계정/역할/티어 등) 탐색 — 사용자에게 확인 (개발 전)
- **1차**: 화면 나열 + 기능 분류 + (코드 있으면) 실존성 검증 + 자체 검증 — 사용자 검토 (개발 전)
- **2차**: 세부 테스트 케이스 작성 + 자동화 타입 분류 (개발 전)
- **3차**: 자동화 코드 변환 + 실행 (**개발 완료 후, 코드 기반 필수**)

**사용자 확인 지점**: `0차 종료` / `1차-A 종료` / `1차-C 종료` / `2차-C 종료` / `3차-A 종료` / `3차-C 종료`. 이 6곳에서만 멈추고 사용자 응답을 기다린다.

**1차-B / 2차-B / 3차-B는 사용자 확인 지점이 아니다.** 1차-B는 화면 최대 5개, 2차-B는 기능 최대 3개를 병렬 처리 후 리포트 한 줄만 남기고 **즉시 세션 종료**. Stop 훅이 다음 배치로 자동 전환. 매 배치마다 "검토해주세요" / "다음 넘어가도 되나요" 같은 질문을 붙이지 않는다.

## 자체 검증 원칙

**사용자에게 보여주기 전에 AI가 먼저 자체 검증을 끝낸다.** 사용자가 일일이 잘못된 걸 걸러내는 일을 하지 않도록, 아래 검증을 AI가 선행한다. 명확히 잘못된 건 수정/제외하고, 판단이 애매한 것만 "확인 필요" 섹션에 모아 사용자에게 질문한다.

## 파일 열기 규칙

스킬이 생성한 마크다운 파일을 사용자에게 보여줄 때는 **VS Code 우선**으로 연다. 없으면 macOS 기본(`open`)으로 폴백한다.

```bash
code "<path>" 2>/dev/null || open "<path>"
```

이 규칙은 0차/1차/2차의 모든 "파일을 연다" 지점에 일괄 적용된다.

---

### 0차: 분리 축(Dimension) 탐색

프로젝트에 테스트 시나리오를 분기시키는 축(계정 유형/역할/티어 등)이 있는지 파악한다. 축이 있다면 **축값 리스트까지 확정**해야 1·2차 설계가 정확해진다.

1. 산출물과 코드 구조를 훑어 축 존재 여부와 축값 리스트를 판단한다.
2. 결과를 채팅에 간단히 출력하고 사용자에게 확인받는다. **파일로 저장하지 않는다.**
   - 축이 있는 경우: "사용자 역할 3계층(system / admin / client)이 있는 것 같은데, 맞나요?"
   - 축이 없는 경우: "계정/티어 구분 없는 단일 사용자 서비스로 보이는데, 맞나요?"

**여기서 반드시 멈추고 사용자의 응답을 기다린다.** 확정된 축값은 1차에서 **축값별로 체크리스트 파일을 분리**하는 기준이 된다. 축이 없으면 체크리스트 파일 1개만 만든다.

---

### 1차: 화면 나열 + 기능 분류 (병렬 배치 방식)

**핵심 원칙**: 모든 화면을 한 번에 분류하지 않는다. 매 스킬 호출은 **화면 최대 5개를 병렬로 처리**하고 세션을 멈춘다. Agent 툴로 서브에이전트를 동시에 띄워 각 화면을 독립 컨텍스트에서 처리한다. 서브에이전트는 파트 파일만 쓰고, 체크리스트 업데이트는 메인이 결과 수집 후 일괄 처리한다.

#### 1차 진입 시 모드 판별

스킬이 호출되면 먼저 체크리스트 파일의 존재 여부와 상태를 확인해 모드를 판별한다.

- **축이 있는 경우**: 축값별 체크리스트 파일. `테스트-리스트-1차-체크리스트-{축값}.md` (예: `-system`, `-admin`, `-client`)
- **축이 없는 경우**: 체크리스트 파일 1개. `테스트-리스트-1차-체크리스트.md`

| 상태 | 모드 | 동작 |
|------|------|------|
| 체크리스트 파일이 하나도 없음 | **1차-A** (Enumerate) | 축값별 화면 목록 생성 후 정지 |
| 파일 있음 + 어느 파일이든 Pending 있음 | **1차-B** (Parallel Iterate) | Pending 최대 5개 병렬 처리 후 정지 |
| 모든 파일에 Pending 없음 | **1차-C** (Consolidate) | 축값별 파트 파일 병합 후 정지 |

---

#### 1차-A: 화면 목록 확정 + 사전 실존성 필터 (1회만 수행)

1. IA(정보구조도)를 기준으로 **모든 화면을 나열**한다. IA 문서가 없으면 애플리케이션 라우팅 구조(또는 사이트맵)를 IA로 대체하고, 그 사실을 명시한다. IA에 있는 화면이 하나도 빠져서는 안 된다.

2. **기능 나열은 하지 않는다.** 이 단계에서는 **화면 이름·경로만** 확정한다. 기능 분류는 1차-B 루프에서 화면별로 처리.

3. **축값별로 접근 가능한 화면을 판단**한다 (축이 있는 경우).
   - 판단 근거: 라우팅 가드(`SystemAdminGuard`, `AdminGuard`, `AuthGuard` 등), 메뉴 구성, 산출물의 권한 매트릭스.
   - 동일 화면이 여러 축값에서 접근 가능하면 **각 축값의 체크리스트에 모두 등록**한다. 축값마다 시나리오가 다를 수 있기 때문 (중복 작성을 허용한다).

4. **사전 실존성 필터 — ①축(라우팅 등록)만 확인** (코드가 있는 프로젝트일 때만. 문서만 있으면 이 단계 건너뜀)
   - 라우팅 정의 파일들(`*.routes.ts`, `app-routing.module.ts` 등)을 읽고, 나열된 화면 경로가 등록되어 있는지 grep/매칭으로 확인한다.
   - **라우팅에 있음**: 체크리스트에 등록. ①축을 ✅로 표기해 1-B에서 재확인하지 않는다.
   - **라우팅에 없음**: 체크리스트에서 제외하고, `테스트-리스트-1차-문서전용화면.md` 에 따로 기록 (문서에는 있는데 구현은 없는 화면 → 사용자 확인용).
   - **깊은 검증(②메뉴/링크, ③컴포넌트, ④가드↔문서)은 여기서 하지 않는다.** 화면별 깊은 판단은 1-B 루프에서 하나씩.

   `테스트-리스트-1차-문서전용화면.md` 형식:
   ```markdown
   # 문서에만 존재하는 화면 (라우팅 미등록)

   | # | 1Depth | 2Depth | 3Depth | 경로 | 산출물 근거 | 비고 |
   |---|--------|--------|--------|------|-------------|------|
   | 1 | ... | ... | ... | /... | IA 문서 p.12 | 구현 여부 확인 필요 |
   ```

5. **축값마다 체크리스트 파일**을 생성한다.
   - 축이 있는 경우: `테스트-리스트-1차-체크리스트-{축값}.md` (예: `-system`, `-admin`, `-client`)
   - 축이 없는 경우: `테스트-리스트-1차-체크리스트.md` 하나만

   각 파일 형식:
   ```markdown
   # 1차 기능 분류 체크리스트 — {축값}

   | # | 1Depth | 2Depth | 3Depth | 경로 | 라우팅 | 상태 | 파트 파일 |
   |---|--------|--------|--------|------|:------:|:----:|-----------|
   | 1 | 홈 | 내 정보 | — | /p/h/me | ✅ | ⬜ Pending | — |
   | 2 | 홈 | 라스트 | 목록 | /p/h/last/list | ✅ | ⬜ Pending | — |
   | ... |
   ```

   - 상태 범례: ⬜ Pending / 🟡 In Progress / ✅ Done
   - 라우팅 컬럼: ✅ (1-A에서 확인됨) / N/A (코드 없는 프로젝트 — 검증 생략)

6. 파일 열기 규칙에 따라 생성된 체크리스트 파일 + 문서전용화면 파일(있으면)을 **모두** 연다.

7. 사용자에게 리포트한다:
   > "축값별 화면 목록을 확정했습니다. 파일 N개(축값별), 총 M행. 라우팅에 없는 화면 K건은 `테스트-리스트-1차-문서전용화면.md` 로 분리했습니다. 누락/과잉 확인해주세요. OK 하시면 화면 최대 5개씩 병렬로 루프를 돌립니다."
   (축이 없으면: "화면 목록 N개를 확정했습니다. …")
   (코드 없는 프로젝트면: "라우팅 사전 필터는 생략했습니다 (코드 없음).")

**여기서 반드시 멈춘다. 루프를 내부에서 시작하지 않는다.** 사용자가 승인하면 다음 스킬 호출에서 1차-B 모드로 진입한다.

---

#### 1차-B: 화면 최대 5개 병렬 처리 (매 스킬 호출당 1배치, Stop 훅으로 반복)

메인 에이전트(오케스트레이터)가 서브에이전트를 최대 5개 동시에 띄운다. 각 서브에이전트는 화면 1개를 독립 컨텍스트에서 처리하고 파트 파일만 쓴다. 메인은 결과 수집 후 체크리스트를 일괄 업데이트한다.

1. **축값 순서대로** 체크리스트 파일을 스캔해 **⬜ Pending 화면을 최대 5개** 선택한다.
   - 순서: 산출물/코드의 역할 정의 순서대로 (예: `system → admin → client`).
   - 현재 축값의 Pending을 모두 소진하면 다음 축값으로 넘어간다.
   - 모든 축값의 Pending이 소진되면 1차-C로 전환.
   - 축이 없는 경우: 체크리스트 파일 1개만 스캔.

2. 선택된 화면 행들의 상태를 모두 🟡 In Progress 로 업데이트한다.

3. **Agent 툴로 서브에이전트를 동시에 띄운다** (`subagent_type: "general-purpose"`). 한 메시지에 모든 서브에이전트 호출을 포함해 병렬 실행한다.

   각 서브에이전트 프롬프트 — 메인 에이전트가 아래 템플릿의 `{명세}` 자리에 이 파일의 `##### 1차-B 서브에이전트 작업 명세` 섹션 전체를 인라인으로 붙여넣어 구성한다 (파일 경로 참조 없음):
   ```
   아래 명세를 따른다:

   {##### 1차-B 서브에이전트 작업 명세 섹션 내용 전체}

   담당 화면:
   - 번호: {N}
   - 축값: {축값}
   - 1Depth/2Depth/3Depth: {값}
   - 경로: {경로}

   산출물 경로: {산출물 디렉토리}
   코드 경로: {프로젝트 루트} (코드 없으면 "없음")
   ```

4. **모든 서브에이전트가 완료되면** 결과를 수집한다.
   - 파트 파일이 생성되었으면 → ✅ Done, 파트 파일 경로 기록
   - 파트 파일이 없으면 (서브에이전트 실패) → ⬜ Pending 으로 **되돌린다**. 다음 배치에서 재처리.

5. 체크리스트 파일의 해당 행들을 일괄 업데이트한다 (✅ Done + 파트 파일 경로).

6. **상태 리포트만** 한 줄로 출력한다. **사용자에게 질문하지 않는다.**
   > "배치 처리 완료: {축값} #N1·#N2·... ({성공}건 성공/{실패}건 재예약). 남은 Pending: 총 M개. 다음 배치는 Stop 훅이 자동 전환합니다."

   **금지 문구 (절대 붙이지 말 것)**:
   - ❌ "검토하시고 추가/수정할 항목 있으면 말씀 주세요"
   - ❌ "다음 화면으로 넘어가도 되면 알려주세요"
   - ❌ "확인 후 진행해도 될까요"
   - ❌ 그 외 사용자 답변을 요구하는 모든 문구

   이유: 1-B는 화면마다 멈추는 단계가 아니다. 화면 단위 검토는 **1-C 전역 병합 시점에 한꺼번에** 한다.

7. **리포트 한 줄 출력 직후 바로 스킬 실행을 종료한다.** 다음 배치를 이 세션 안에서 이어서 처리하지 않는다. **Stop 훅이 체크리스트 파일들을 확인해 Pending이 남아있으면 스킬을 재호출**한다.

##### 1차-B 서브에이전트 작업 명세

**이 섹션은 서브에이전트 전용이다.** 메인 에이전트가 이 내용을 서브에이전트 프롬프트에 직접 인라인으로 붙여넣는다. 서브에이전트는 이 명세만 따른다.

**작업**: 담당 화면 1개에 대해 기능 분류 + 파트 파일 저장. 완료 후 80단어 이내 리포트.

**단계 1 — 기능 분류**:
- 산출물에서 해당 화면에 해당하는 구간만 집중 읽는다 (전체 산출물 재탐색 금지).
- 해당 축값의 관점에서, 그 축값이 실제로 사용 가능한 기능만 기록한다.
- 각 기능마다 근거(산출물명 + 위치)를 명시한다. 근거 없는 항목은 제외.

**단계 2 — 심층 실존성 검증** (코드가 없으면 건너뜀):
①축(라우팅)은 1-A에서 이미 확인됨. ②③④축만 점검:

| 축 | 확인 방법 | 결과 |
|----|-----------|------|
| ② 메뉴/링크 접근 | 메뉴 설정 + 내부 네비게이션(`navigate()`, `routerLink`). **현재 축값이 실제로 볼 수 있는 것만 인정** | ✅/❌/⚠️(직접 URL만) |
| ③ 컴포넌트 실구현 | 컴포넌트 HTML/TS 최소 스캔, 기능이 UI/핸들러로 존재하는지 | ✅/부분/❌ |
| ④ 가드 ↔ 문서 일치 | `canActivate` 가 현재 축값을 허용/차단하는지, 문서 권한 매트릭스와 일치하는지 | ✅/❌ |

| 패턴 | 판정 | 조치 |
|------|------|------|
| ②·③·④ 모두 ✅ | 테스트 대상 | 기능 목록 그대로 |
| ② = ❌ (메뉴·링크 없음) | **자동 제외** | 파트 파일에 `제외됨: 라우팅은 있으나 진입 경로 없음` 명기, 기능 목록 비움 |
| ② = 직접 URL만 (⚠️) | 확인 필요 | "확인 필요" 섹션에 기록 |
| ③ = 부분 또는 ❌ | 확인 필요 | 구현된 기능만 남기고 나머지는 "확인 필요"에 |
| ④ = ❌ | 확인 필요 | 문서·코드 중 뭐가 맞는지 "확인 필요"에 |

**단계 3 — 화면 범위 자체 검증** (사용자에게 넘기기 전에 AI가 선제적으로 걸러냄):
- [ ] 각 기능에 근거(산출물명 + 위치)가 달려있는가
- [ ] 포함 관계 중복 없는가 (예: "사이트 접속" + "로그인" → 로그인만 남김)
- [ ] **기능 단위 원칙** — 기능 1개 = **고객사가 개발자에게 별도로 요청했을 법한 사용자 목적** 단위.
  판별 질문: "고객이 기획 미팅에서 '이것도 별도로 넣어달라'고 말했을 것인가?" → **아니오**이면 상위 기능에 흡수.
  - **흡수 대상 예시** (독립 기능 X):
    - 입력 필드 자체: "아이디 입력", "비밀번호 입력" → "로그인"에 포함
    - 폼 제출의 성공/실패 분기: "실패 응답 처리", "로그인 성공 후 이동" → "로그인"에 포함
    - 입력 제약 구현: "한글 입력 차단", "이메일 형식 검증", "폼 검증 메시지" → "로그인"에 포함 (구현 세부사항)
    - 당연한 에러 핸들링: 어떤 기능이든 에러는 처리해야 하므로, 에러 처리 자체는 기능이 아님
  - **하나의 폼 = 하나의 기능**: 여러 필드(구분 선택·아이디·비밀번호·이름·번호 등)를 채워 제출하는 흐름은 필드별로 쪼개지 않고 하나로 통합한다. 예: "사용자 구분 선택" + "아이디 중복 검증" + "사용자 추가" → "사용자 추가 (구분·아이디·비밀번호·이름·휴대폰)"
  - **입력 형식 조건은 별도 기능으로 등록**: 고객이 명시적으로 요청한 입력 규칙(이메일 형식, 비밀번호 8자 이상 영문·숫자 조합, 숫자만 입력 등)은 폼 제출 기능과 분리해 별도 행으로 등록한다. 로그인·회원가입·비밀번호 변경·사용자 추가 등 인증 관련 화면에서 공통 적용.
  - **브라우저·OS 기본 UX도 흡수 대상**: 브라우저나 OS가 암묵적으로 제공하거나 당연히 포함되는 UX는 고객이 별도 요청하지 않았으므로 독립 기능이 아님. 고객이 명시적으로 요청한 경우에만 예외.
- [ ] **자명한 기능 제외** — 다음은 케이스로 만들지 않는다:
  - OS/브라우저 기본 동작: 가로 스크롤 허용, Tab 키 포커스 이동, 마우스 호버 등
  - **negative 자명 동작**: "행 클릭 무동작", "비활성화 시 입력 막힘" 등 (동작 안 하는 게 자명)
  - 화면 내 네비게이션: 뒤로가기 버튼, 탭 이동 (해당 화면 기능 검증 과정에서 자연 검증)
  - **화면 간 진입(이동) 버튼**: 목적지 화면이 **이 버튼에서만 접근 가능**한 경우 → 그 화면 테스트로 자연 검증되므로 버튼 자체는 기능으로 등록하지 않는다. 목적지 화면이 **다른 경로로도 접근 가능**한 경우 → 그 화면 테스트가 통과해도 이 버튼이 동작하는지 알 수 없으므로 기능으로 등록한다. 예: 목록 → 추가 화면(추가 버튼에서만 진입) → 제외. 사용자 정보 → 이력 화면(메뉴에서도 접근 가능) → 포함.
- [ ] 기능 이름이 산출물 용어와 일치하는가 (이름 일관성)
- [ ] UI 디테일(레이아웃·색·애니메이션 등) 기능으로 오인되어 들어가진 않았는가
- [ ] **정적 표시 제외** — 다음은 케이스로 만들지 않는다:
  - 텍스트/숫자 표시: "총 N건 표시", "버전 표시", "안내 문구 표시"
  - 단순 컬럼/필드 존재 표시: "X 컬럼이 보인다" 류 (값 검증이 아닌 단순 노출)
  - 정적 상태 표시: "빈 상태 메시지 표시", "총 건수 칩 표시"
  - 단, 해당 표시가 기능 동작의 결과(예: 검색 후 N건 표시 → 검색 기능)이면 그 기능에 포함시키되 별도 케이스로는 분리하지 않는다.
- [ ] **컬럼/필드별 분리 통합** — 컬럼·필드별로 케이스가 따로 있다면 1개로 합치고 비고에 나열한다. 예: "활동 컬럼" + "사용자 컬럼" + "시각 컬럼" → "이력 테이블 컬럼 표시 (컬럼: 활동, 사용자, 시각, ...)"
- [ ] **다른 축값에서 같은 이름이지만 흐름이 다른 기능이 있을 가능성은 없는가** — 있다면 **다른 이름으로 분리**해 적는다. 예: client의 `내 정보 수정 (본인만)` vs system의 `사용자 정보 수정 (전체)`. 2차에서 단순 통합되어야 하므로 이름이 같으면 같은 흐름이라는 약속을 1차에서 보장한다.
- [ ] **화면 시드(다른 화면이 만들어준 사전 데이터)가 명시되었는가** — 이 화면이 동작하려면 미리 있어야 할 데이터가 있는데도 시드가 비어있다면 누락. 예: 비교파일 화면 → 라스트 2개 이상 시드 필수.
- [ ] **테스트 대상이 없는 화면은 문서에서 제외** — 라우팅은 등록되어 있으나 실제 테스트할 기능이 없는 화면(예: 상세 페이지가 별도 UI 없이 목록 화면에 통합된 경우)은 "해당 없음"으로 표기하지 말고 아예 목록에서 제외한다.
- [ ] **기능의 핵심 결과(상태 변화·UI 전환·피드백)가 비고에 명시되었는가** — 2차는 1차를 입력으로 사용하며 코드를 재분석하지 않는다. 기능 실행 후 어떤 상태로 바뀌는지(예: 버튼 레이블 전환, 스낵바 메시지, 리다이렉트 등)가 비고에 없으면 2차에서 검증 포인트를 놓친다. 기능명만으로 결과가 자명하지 않은 경우 반드시 명시.

자체 검증에서 걸린 항목은 AI가 **바로 제거/수정**한다. 애매한 건 파트 파일 하단 "확인 필요" 섹션으로.

**단계 4 — 파트 파일 저장**:
- 축이 있는 경우: `테스트-리스트-1차-partials/{축값}/{번호}-{2Depth}-{3Depth}.md`
- 축이 없는 경우: `테스트-리스트-1차-partials/{번호}-{2Depth}-{3Depth}.md`

파일 형식:
```markdown
# [#N] {축값} — 1Depth > 2Depth > 3Depth (경로)

## 화면 시드 (사전 데이터)
이 화면이 정상 동작하려면 진입 시점에 어떤 데이터가 미리 있어야 하는가? **다른 화면이 만들어줘야 하는 데이터만** 적는다 (이 화면 자체에서 만드는 데이터는 X). 없으면 "없음".
예: 라스트 2개 이상 / 비교파일 1개 이상 / admin 사용자 1명 이상

## 기능
| 기능 | 근거 | 비고 |
|------|------|------|
| ... |

## 확인 필요
- (AI가 판단 못한 애매한 것만. 없으면 섹션 생략)
```

> **실존성 검증 결과 / 자체 검증 결과는 산출물에 적지 않는다.** 모델이 거를 때만 쓰는 작업 메타데이터. 검증 통과 여부는 직접 반영(테스트 대상 포함/제외)하고, 사용자가 봐야 하는 모호함만 "확인 필요" 섹션에 모은다.

**완료 리포트** (80단어 이내, 채팅에 출력):
- 화면명/경로, 기능 N건 작성, 확인필요 K건, 파트 파일 경로.
- **사용자에게 질문하지 않는다.** 이 리포트는 메인 에이전트가 수집하는 용도이며 사용자에게 직접 보이지 않는다.

##### Stop 훅 설정 (사용자 환경에 1회 등록)

`.claude/settings.json` 의 Stop 훅에 아래 로직을 등록해야 루프가 자동 반복된다. **훅이 없으면 사용자가 수동으로 "다음" 이라고 해야 한다.**

개념:
```
Stop 훅 발동
  → 모든 `테스트-리스트-1차-체크리스트*.md` 파일에 ⬜ Pending 행이 있는지 확인
    → 있으면: 스킬 재호출 (새 세션, 새 컨텍스트)
    → 없으면: 훅 종료 (정상 완료)
```

스킬 자체는 훅을 설정하지 않는다. 사용자가 "훅 세팅해줘" 라고 하면 그때 `update-config` 스킬로 `.claude/settings.json` 을 편집한다.

---

#### 1차-C: 병합 + 전역 자체 검증 (모든 Pending 소진 시)

1. **축값별로 최종 파일을 생성**한다. 해당 축값 폴더의 파트 파일들을 병합.
   - 축이 있는 경우: `테스트-리스트-1차-기능분류-{축값}.md` (축값마다 1개)
   - 축이 없는 경우: `테스트-리스트-1차-기능분류.md` 1개

   파일 첫 줄에 헤더 명시:
   ```markdown
   <!-- 작성일: YYYY-MM-DD / 기준 문서: {산출물명} v{버전 또는 일자} -->
   ```
   3차 진입 시 변경사항 점검의 기준이 됨.

   각 파일 컬럼:
   ```
   | 1Depth | 2Depth | 3Depth | 경로 | 기능 | 근거 | 비고 |
   ```

   화면 정보와 기능 분류만 적는다. 실존성/자체 검증 결과는 작업 메타데이터라 산출물에 포함하지 않는다.

2. **전역 자체 검증** (여러 화면에 걸친 검증 — 1차-B 단일 화면 검증에서 잡을 수 없는 것들)
   - [ ] IA에 있는 화면이 체크리스트에서 빠지지 않았는가
   - [ ] 동일 기능이 여러 화면에 다른 이름으로 적혀있지 않은가 (naming 일관성)
   - [ ] 포함관계 중복이 화면 경계 넘어서는 없는가 (예: 공통 레이아웃 기능이 여러 화면에 중복 기록되지 않음)
   - [ ] 산출물에 있는데 1차 결과물에 누락된 화면·기능은 없는가
   - [ ] 축이 있는 경우: 같은 기능이 여러 축값 파일에 일관된 내용으로 기록되었는가

   명확히 잘못된 건 AI가 **바로 수정**. 애매한 건 `테스트-리스트-1차-자체검증.md` 에 "확인 필요" 섹션으로 모음.

3. 파일 열기 규칙에 따라 최종 파일과 자체검증 파일을 **모두** 연다.

4. **스냅샷 저장** — `테스트-리스트-스냅샷.json` 생성/업데이트:
   ```json
   {
     "1차": {
       "completed_at": "YYYY-MM-DD",
       "source_hashes": {
         "docs/IA.md": "<sha256>",
         "docs/PRD.xlsx": "<sha256>"
       }
     }
   }
   ```
   산출물 디렉토리의 모든 문서 파일 해시를 기록. 증분 재실행 시 변경 감지 기준점으로 사용.

5. 사용자에게 리포트:
   > "1차 완료. 축값별 파일 N개, 총 M개 화면, K개 기능 행. AI 자체 검증에서 X건 자동 수정, Y건 확인 필요. 자체검증 파일부터 보시면 됩니다."

**여기서 멈춘다.** 승인되면 2차로 진행한다.

---

### 2차: 세부 테스트 케이스 작성 (병렬 배치 방식)

1차 기능 분류가 승인되면 진입한다. **모든 기능을 한 번에 처리하지 않는다.** 매 스킬 호출은 **기능 최대 3개를 병렬로 처리**하고 세션을 멈춘다. Agent 툴로 서브에이전트를 동시에 띄워 각 기능을 독립 컨텍스트에서 처리한다. 서브에이전트는 파트 파일만 쓰고, 체크리스트 업데이트는 메인이 결과 수집 후 일괄 처리한다.

#### 2차 진입 시 모드 판별

스킬이 호출되면 먼저 2차 체크리스트 파일의 존재 여부와 상태를 확인해 모드를 판별한다.

| 상태 | 모드 | 동작 |
|------|------|------|
| `테스트-리스트-2차-체크리스트.md` 없음 | **2차-A** (Enumerate) | 기능 통합 + 대표 축값 매핑 후 정지 |
| 체크리스트에 ⬜ Pending 있음 | **2차-B** (Parallel Iterate) | Pending 최대 3개 병렬 처리 후 정지 |
| Pending 없음 | **2차-C** (Consolidate) | 축값별 파일 병합 + 자체검토 후 정지 |

---

#### 2차-A: 기능 체크리스트 생성 + 대표 축값 매핑 (1회만 수행)

1. 1차 최종 파일(`테스트-리스트-1차-기능분류-{축값}.md` 또는 `테스트-리스트-1차-기능분류.md`)을 읽어, **모든 (화면, 기능) 행을 통합**한다. 같은 (화면, 기능)이 여러 축값 파일에 등장하면 1행으로 통합.

2. **권한 매트릭스 도출** (축이 있는 경우):
   - 같은 (화면, 기능)이 어떤 축값 파일에 등장 → 그 축값에서 **허용(✅)**.
   - 어느 축값 파일에도 없음 → 그 축값에서는 **차단(❌)**.
   - 1차-B의 ④가드↔문서 일치 검증 결과를 신뢰한다 (1차에서 이미 거른 것).

3. **대표 축값 결정 + 자동화 타입 분류** (대표 축값은 축이 있을 때만, 자동화 타입은 축 무관 항상):

   **대표 축값:**
   - 기본 규칙: 해당 기능을 허용받는 축값 중 **가장 높은 권한의 축값** (예: `system → admin → client` 순).
   - 같은 기능 이름인데 축값별로 흐름이 미묘하게 다를 가능성이 보이는 케이스(예: "내 정보 수정"이 client는 본인만, system은 전체)는 통합하지 말고 **분리해 등록**하거나 "확인 필요"에 모은다.

   **자동화 타입 (API 우선 원칙)**:
   - **API**: UI 없이 백엔드만 검증해도 충분한 기능 — 대부분의 CRUD, 검증 로직, 권한 차단(API 거부) 등. **기본값**.
   - **UI**: UI 표시/숨김, 라우팅 가드(URL 직접 진입 차단), 시각 검증, 사용자 인터랙션(드래그·줌)이 본질인 기능.
   - **수동**: 자동화가 매우 어렵거나 비용 큰 기능 — 3D 뷰어 시각 검증, 외부 시스템 연계, OS 권한 등.

4. **자체 검증** (사용자에게 넘기기 전 AI가 선제 점검):
   - [ ] 1차에 있던 (화면, 기능)이 누락되지 않았는가
   - [ ] 같은 화면 내 동명 기능이 중복 등록되지 않았는가 (1행으로 통합)
   - [ ] 권한 매트릭스가 모든 축값에 대해 채워져 있는가
   - [ ] 대표 축값이 권한 매트릭스에서 ✅ 인 축값 중 하나로 선택되었는가
   - [ ] 자동화 타입이 모든 기능에 채워져 있는가 (API 우선 원칙 적용)
   - 명확히 잘못된 건 AI가 바로 수정. 애매한 건 `테스트-리스트-2차-A-자체검증.md` 의 "확인 필요" 섹션에 모음.

5. `테스트-리스트-2차-체크리스트.md` 생성:

   **축이 있는 경우**:
   ```markdown
   # 2차 기능별 테스트 케이스 체크리스트

   | # | 화면 | 기능 | 대표 축값 | 권한(system/admin/client) | 자동화 | 상태 | 파트 파일 |
   |---|------|------|-----------|:-------------------------:|:------:|:----:|-----------|
   | 1 | 라스트 목록 | 라스트 검색 | system | ✅/✅/✅ | API | ⬜ Pending | — |
   | 2 | 사용자 관리 | 사용자 추가 | system | ✅/❌/❌ | API | ⬜ Pending | — |
   ```

   **축이 없는 경우**: `대표 축값` / `권한` 컬럼 생략. `자동화` 컬럼은 유지.

   상태 범례: ⬜ Pending / 🟡 In Progress / ✅ Done

6. 파일 열기 규칙에 따라 체크리스트 파일을 연다. "확인 필요"가 있으면 자체검증 파일도 함께. (사용자가 백그라운드에서 볼 수 있게 열어둘 뿐, 멈추지 않는다.)

7. **곧바로 같은 세션에서 2차-B의 첫 배치(최대 3개)까지 처리한다. 사용자 확인을 받지 않는다.**
   - 1차 결과는 1-C에서 이미 사용자가 검토했으므로, 2-A는 기계적 가공만 하는 단계(dedupe + 권한 매트릭스 도출 + 대표 축값 자동 결정). **새로운 판단이 들어가지 않으므로 별도 확인 단계 불필요.**
   - 첫 배치를 같은 세션에서 처리해 ✅ 마커를 찍어둬야 Stop 훅의 안전 가드(`✅|🟡` 마커 존재 조건)를 통과해 다음 세션부터 자동 루프가 작동한다. 2-A 자체는 1차 파일 통합만 하는 가벼운 단계라 컨텍스트 누적이 작다.
   - 이 세션의 출력 = "2-A 완료(통합 N건/확인필요 K건) + 2-B 첫 배치 M건 처리 완료" 한두 줄. (2-B의 자세한 절차는 다음 섹션 참조)
   - "확인 필요" 항목은 자체검증 파일에 모아두고, 2-C 검토 시 함께 본다.

8. 두 번째 배치부터는 Stop 훅이 ⬜ Pending 을 감지해 다음 세션에서 2차-B 모드로 자동 진입.

---

#### 2차-B: 기능 최대 3개 병렬 처리 (매 스킬 호출당 1배치, Stop 훅으로 반복)

메인 에이전트(오케스트레이터)가 서브에이전트를 최대 3개 동시에 띄운다. 각 서브에이전트는 기능 1개를 독립 컨텍스트에서 처리하고 파트 파일만 쓴다. 메인은 결과 수집 후 체크리스트를 일괄 업데이트한다.

1. `테스트-리스트-2차-체크리스트.md` 에서 **⬜ Pending 기능을 최대 3개** 선택한다. 모든 Pending이 소진되면 2차-C로 전환.

2. 선택된 행들의 상태를 모두 🟡 In Progress 로 업데이트.

3. **Agent 툴로 서브에이전트를 동시에 띄운다** (`subagent_type: "general-purpose"`). 한 메시지에 모든 서브에이전트 호출을 포함해 병렬 실행한다.

   각 서브에이전트 프롬프트 — 메인 에이전트가 아래 템플릿의 `{명세}` 자리에 이 파일의 `##### 2차-B 서브에이전트 작업 명세` 섹션 전체를 인라인으로 붙여넣어 구성한다 (파일 경로 참조 없음):
   ```
   아래 명세를 따른다:

   {##### 2차-B 서브에이전트 작업 명세 섹션 내용 전체}

   담당 기능:
   - 번호: {N}
   - 화면: {화면}
   - 기능: {기능}
   - 대표 축값: {축값}
   - 권한 매트릭스: {system=✅/❌ / admin=✅/❌ / client=✅/❌}
   - 자동화 타입: {API/UI/수동}

   산출물 경로: {산출물 디렉토리}
   1차 기능분류 파일: {경로}
   ```

4. **모든 서브에이전트가 완료되면** 결과를 수집한다.
   - 파트 파일이 생성되었으면 → ✅ Done, 파트 파일 경로 기록
   - 파트 파일이 없으면 (서브에이전트 실패) → ⬜ Pending 으로 **되돌린다**. 다음 배치에서 재처리.

5. 체크리스트 파일의 해당 행들을 일괄 업데이트한다 (✅ Done + 파트 파일 경로).

6. **상태 리포트만 한 줄 출력. 사용자에게 질문하지 않는다.**
   > "배치 처리 완료: #N1·#N2·... ({성공}건 성공/{실패}건 재예약). 남은 Pending: M개. 다음 배치는 Stop 훅이 자동 전환합니다."

   **금지 문구** (1차-B와 동일):
   - ❌ "검토하시고 추가/수정할 항목 있으면 말씀 주세요"
   - ❌ "다음 기능으로 넘어가도 되면 알려주세요"
   - ❌ "확인 후 진행해도 될까요"
   - ❌ 그 외 사용자 답변을 요구하는 모든 문구

   이유: 2차-B는 기능마다 멈추는 단계가 아니다. 전역 검토는 **2차-C 시점에 한꺼번에** 한다.

7. **리포트 출력 직후 즉시 스킬 실행 종료.** 다음 배치를 이 세션에서 이어서 처리하지 않는다. **Stop 훅이 체크리스트 파일을 보고 Pending이 남아있으면 스킬을 재호출**한다.

##### 2차-B 서브에이전트 작업 명세

**이 섹션은 서브에이전트 전용이다.** 메인 에이전트가 이 내용을 서브에이전트 프롬프트에 직접 인라인으로 붙여넣는다. 서브에이전트는 이 명세만 따른다.

**작업**: 담당 기능 1개에 대해 (A) 기능심화 케이스 + (B) 권한확인 케이스 작성 + 파트 파일 저장. 완료 후 80단어 이내 리포트.

**단계 1 — (A) 기능 심화 케이스 (대표 축값 1개로만 작성)**:
- **정상 흐름** 케이스 (1건 이상)
- **예외 분기** 케이스 (입력 검증 실패, 빈 값/한도, 잘못된 상태, 중복 등)
- **장애 유발** 가능 시나리오 (네트워크 오류, 동시성 충돌, 세션 만료 중 작업, 권한 변경 등)
- 각 케이스를 [상황] → [행동] → [결과] 형식으로 구체화.
- 산출물 근거(이름 + 위치) 명시.
- 역할이 달라져도 백엔드 로직은 같으므로, 심화 시나리오를 역할마다 반복하지 않는다.

**자명한 prereq/step 생략 원칙**:
- **자명한 prereq**(로그인된 상태, 화면에 진입한 상태 등)는 [상황]에 매번 적지 않는다. 화면 헤더의 시드/기본 가정에 흡수.
- **자명한 step**(예: "로그인 버튼을 누른다 → 로그인된다"의 "버튼 누르기")은 [행동]에 별도 케이스로 분리하지 않는다. 본 검증만 기재.
- **다른 화면이 만들어준 데이터**(예: 비교파일 화면에서 라스트 2개 필요)는 [상황]에 매번 적지 말고, **파트 파일 헤더의 "화면 시드"** 에 흡수. 케이스는 시드가 갖춰진 상태에서 시작한다고 가정.
- 1차의 "자명한 기능 제외" 원칙을 케이스 단위에도 그대로 적용.

**자동화 타입별 작성 형식** (도구 무관 표현):
- **API 케이스**: [행동]에 `METHOD path` + 입력 키, [결과]에 status code + 응답 검증 키. 예: `POST /api/users {name, email}` → `201, body.id 존재`.
- **UI 케이스**: [행동]에 의미적 행위(역할/라벨), [결과]에 의미적 단언(visible/text/disabled). 예: `[버튼:"추가"] 클릭` → `[다이얼로그:"라스트 추가"] 보임`. selector 코드(`#btn-add` 등) 직접 표기 X.
- **수동 케이스**: 비고에 자동화 어려운 사유 명시.

**단계 2 — (B) 권한 확인 케이스 (모든 축값에 대해 작성, 축이 있는 경우만)**:
- 권한 매트릭스 ✅ 인 축값 → **허용 케이스 1건** (smoke level: 접근 성공 + 기능 기본 동작 확인)
- 권한 매트릭스 ❌ 인 축값 → **차단 케이스 3건** (3중 검증 모두):
  1. UI 상에서 버튼/메뉴 숨김
  2. 화면 경로 직접 진입(URL 타이핑) 시 차단/리다이렉트
  3. 백엔드 API 직접 호출 시 거부 (권한 에러 반환)
- 축이 없는 경우 (B) 단계 생략.

**단계 3 — 자체 검증** (사용자에게 넘기기 전 AI가 선제 점검):
- [ ] 기능심화에 정상/예외/장애 시나리오가 모두 포함되어 있는가
- [ ] 차단 케이스가 3중 검증(UI/경로/API)을 빠짐없이 포함하는가
- [ ] 기대 결과가 모호하지 않고 검증 가능한가 (모호한 표현 금지)
- [ ] 자명한 케이스 섞여있지 않은가 — 1차의 자명 제외 기준(OS/브라우저 기본, negative 자명 동작, 화면 내 네비게이션, 정적 표시)을 그대로 적용
- [ ] 포함 관계 중복(앞 케이스가 뒤 케이스에 포함됨)이 없는가
- [ ] **같은 검증의 필드별 반복**은 1개로 통합되었는가 — 예: "라스트명 미입력", "사이즈 미입력", "모델 미입력" → "필수 입력 누락 검증 (필드: 라스트명·사이즈·모델·...)"
- [ ] 모든 케이스에 산출물 근거가 달려있는가
- [ ] [상황]에 자명한 prereq(로그인·화면 진입 등)나 화면 시드에 흡수 가능한 사전 데이터가 중복 명시되지 않았는가
- [ ] [행동]에 자명한 step(버튼 누르기 등)이 본 검증과 분리되어 적혀있지 않은가
- [ ] 자동화 타입과 케이스 형식이 일치하는가 (예: API 분류인데 [버튼 클릭] 같은 UI 형식으로 적힘 X)
- 자체 검증에서 걸린 항목은 AI가 **바로 제거/수정**. 애매한 건 파트 파일 하단 "확인 필요" 섹션으로.

**단계 4 — 파트 파일 저장**: `테스트-리스트-2차-partials/{번호}-{화면}-{기능}.md`

```markdown
# [#N] {화면} > {기능}

- 대표 축값: {축값}
- 권한 매트릭스: system=✅ / admin=❌ / client=❌
- **화면 시드 (사전 데이터)**: 1차에서 정의한 화면 시드를 그대로 옮긴다. 모든 케이스 [상황]은 이 시드가 갖춰진 상태를 가정. 없으면 "없음".

## (A) 기능 심화 케이스 — {대표 축값}

| ID | 분류 | 시나리오 | 상황 | 행동 | 기대 결과 | 근거 |
|----|------|----------|------|------|-----------|------|
| A-1 | 정상 | ... | ... | ... | ... | ... |
| A-2 | 예외 | ... | ... | ... | ... | ... |
| A-3 | 장애 | ... | ... | ... | ... | ... |

## (B) 권한 확인 케이스

| ID | 축값 | 유형 | 시나리오 | 상황 | 행동 | 기대 결과 | 근거 |
|----|------|------|----------|------|------|-----------|------|
| B-1 | system | 허용 | ... | ... | ... | ... | ... |
| B-2 | admin | 차단(UI숨김) | ... | ... | ... | ... | ... |
| B-3 | admin | 차단(경로) | ... | ... | ... | ... | ... |
| B-4 | admin | 차단(API) | ... | ... | ... | ... | ... |
| B-5 | client | 차단(UI숨김) | ... | ... | ... | ... | ... |
| ... |

## 자체 검증 결과
- 제거한 항목: (없으면 "없음")
- 수정한 항목: (없으면 "없음")

## 확인 필요
- (AI가 판단 못한 애매한 것만. 없으면 섹션 생략)
```

**완료 리포트** (80단어 이내, 채팅에 출력):
- 기능명, 케이스 N건 생성 (A:a / B:b), 확인필요 K건, 파트 파일 경로.
- **사용자에게 질문하지 않는다.** 이 리포트는 메인 에이전트가 수집하는 용도이며 사용자에게 직접 보이지 않는다.

##### Stop 훅 호환

기존 1차-B용 Stop 훅이 `테스트-리스트-?차-체크리스트*.md` 또는 `테스트-리스트-*-체크리스트*.md` 패턴으로 Pending을 감지하면 2차-B도 같은 훅으로 자동 반복된다. 1차 전용 패턴(`-1차-`)으로만 등록되어 있다면 2차 시작 전에 패턴을 일반화하도록 수정 필요. 사용자가 "훅 갱신해줘" 라고 하면 그때 `update-config` 스킬로 `.claude/settings.json` 을 편집한다.

---

#### 2차-C: 축값별 파일 병합 + 전역 자체 검증 (모든 Pending 소진 시)

##### 처리 방식

| partials 수 | 처리 |
|-------------|------|
| ≤ 100 | 한 세션에 모든 축값 파일 + 자체검토 일괄 |
| > 100 | **축값 1개씩 별도 세션 (Ralph Loop)**. 진행 상태는 `테스트-리스트-2차-체크리스트-C.md` 에 ⬜/🟡/✅ 마커로 기록 (1차-B/2차-B와 동일 패턴). |

진행 파일 형식 (축이 있는 경우):

```markdown
| # | 단계 | 상태 |
|---|------|------|
| 1 | system 축값 파일 생성 | ⬜ Pending |
| 2 | admin 축값 파일 생성 | ⬜ Pending |
| 3 | client 축값 파일 생성 | ⬜ Pending |
| 4 | 자체검토 + 사용자 리포트 | ⬜ Pending |
```

진입 시 모드 판별:
- 진행 파일 없음 + partials ≤ 100 → 단일 세션 일괄 처리 (아래 본 처리 1~5단계 모두 수행)
- 진행 파일 없음 + partials > 100 → 진행 파일 생성 + **첫 단계까지 같은 세션에서 처리 후 ✅ 마킹** + 즉시 세션 종료 (✅ 1개 찍어둬야 Stop 훅 안전 가드 통과)
- 진행 파일 있음 + ⬜ 있음 → 첫 ⬜ 단계만 처리 후 ✅ 마킹 + 즉시 세션 종료
- 진행 파일 있음 + 모두 ✅ → 정상 종료

진행 파일이 Stop 훅 패턴(`테스트-리스트-?차-체크리스트*.md`)에 매칭되어 **다음 세션부터 자동 루프** 작동. 사용자 트리거는 **첫 호출 1번**만. 마지막 자체검토 단계에서 사용자 리포트 + 종료.

대량 partials를 처리할 때 **모델이 직접 Read 하지 말고 `awk`/`grep`/Python 일괄 추출**을 사용한다 (컨텍스트 보호).

##### 분배 로직 (정확히 한 줄로)

- 파트 파일 1개 = **(A) 표 1개 + (B) 표 1개**
- (A) 표 행 전부 → 파일 헤더의 **"대표 축값"** 에 해당하는 축값 파일로
- (B) 표 행 각각 → 그 행의 **"축값" 컬럼** 값에 해당하는 축값 파일로

---

1. 파트 파일들을 **축값별 최종 파일**로 분배·병합.

   **축이 있는 경우**: `테스트-리스트-2차-{축값}.md` (축값마다 1개)
   - (A) 기능심화 케이스: 그 축값이 **대표인 기능**의 (A) 케이스만 기록
   - (B) 권한확인 케이스: 그 축값에 해당하는 (B) 행만 기록
   - 즉 각 축값 파일엔 **그 역할이 직접 수행할 모든 케이스**가 모인다.

   파일 첫 줄에 헤더 명시:
   ```markdown
   <!-- 작성일: YYYY-MM-DD / 기준 문서: {산출물명} v{버전 또는 일자} -->
   ```
   3차 진입 시 변경사항 점검의 기준이 됨.

   파일 컬럼:
   ```
   | ID | 화면 | 기능 | 케이스 유형 | 자동화 | 시나리오 | 상황 | 행동 | 기대 결과 | 근거 |
   ```
   - 케이스 유형: `기능심화(A)` / `권한확인-허용(B1)` / `권한확인-차단-UI(B2-1)` / `권한확인-차단-경로(B2-2)` / `권한확인-차단-API(B2-3)`
   - 자동화: `API` / `UI` / `수동`
   - 파일 자체가 축값을 나타내므로 별도 "역할" 컬럼은 두지 않는다.

   **축이 없는 경우**: `테스트-리스트-2차.md` 1개. (A) 케이스만.
   ```
   | ID | 화면 | 기능 | 시나리오 | 상황 | 행동 | 기대 결과 | 근거 |
   ```

2. **전역 자체 검증** — `테스트-리스트-2차-자체검토.md` 작성:

   | 항목 | 결과 | 비고 |
   |------|------|------|
   | IA의 모든 화면이 커버되었는가 | O/X | |
   | 1차의 모든 (화면, 기능)이 케이스화되었는가 | O/X | |
   | 체크리스트 ✅ 수 == partials 파일 수 == 최종 파일 (화면, 기능) 묶음 합계 | O/X | 숫자 명시 |
   | 각 기능에 정상/예외/장애 시나리오가 모두 있는가 | O/X | |
   | 포함 관계 중복이 제거되었는가 | O/X | |
   | 자명한 케이스가 제거되었는가 | O/X | |
   | 기대 결과가 모두 구체적인가 | O/X | |
   | 산출물 근거가 없는 케이스가 포함되어 있지 않은가 | O/X | |
   | (축 있음) 각 축값의 허용/차단 케이스가 빠짐없이 있는가 | O/X | |
   | (축 있음) 차단 케이스가 3중 검증(UI/경로/API)을 포함하는가 | O/X | |
   | (축 있음) 기능심화가 대표 축값 파일에만 기록되었는가 (중복 없음) | O/X | |

   X 항목은 즉시 보완 후 다시 검토.

3. **요약 작성**:
   - 전체 케이스 건수
   - 화면별 케이스 수
   - (축 있는 경우) 축값별 케이스 수 (파일별 카운트)
   - 사람이 직접 확인해야 하는 항목과 사유

4. 파일 열기 규칙에 따라 축값별 파일과 자체검토 파일을 **모두** 연다.

5. **스냅샷 저장** — `테스트-리스트-스냅샷.json` 의 `"2차"` 키 추가/업데이트:
   ```json
   "2차": { "completed_at": "YYYY-MM-DD" }
   ```

6. 사용자에게 리포트:
   > "2차 완료. 축값별 파일 N개, 총 K건 케이스. 자체 검토 X건 통과 / Y건 확인 필요. 자체검토 파일부터 보시면 됩니다."

**여기서 멈춘다.** 끝.

---

### 3차: 자동화 코드 변환 + 실행 (개발 완료 후, 코드 기반 필수)

> **선결 조건**: 코드(라우팅·컴포넌트·API 핸들러·DB 모델 등)가 존재해야 진입 가능. 코드 없는 프로젝트는 3차 진행 불가 — 사용자에게 알리고 종료.

#### 3차 진입 시 모드 판별

| 상태 | 모드 | 동작 |
|------|------|------|
| `테스트-리스트-3차-체크리스트.md` 없음 | **3차-A** (변경사항 점검 + 환경 수집) | 점검·수집·변환대상 결정 후 첫 단위까지 처리 |
| 체크리스트에 ⬜ Pending 있음 | **3차-B** (Parallel Iterate) | 케이스 최대 3개 병렬 변환 후 정지 |
| Pending 없음 | **3차-C** (Consolidate) | 실행 + 결과 보고 후 정지 |

---

#### 3차-A: 변경사항 점검 + 자동화 환경 정보 수집

1. **변경사항 점검**:
   - 1차 통합 파일과 2차 최종 파일 헤더(작성일자·기준문서) 확인.
   - 현재 기획 문서 비교 → 추가/변경/제거된 화면·기능 식별.
   - 코드(라우팅·컴포넌트·API) 비교 → 산출물에 없는 신규 코드 또는 산출물에 있는데 코드에 없는 기능 식별.
   - 차이가 있으면 `테스트-리스트-3차-변경점검.md` 에 기록하고 **사용자에게 보고 후 멈춤** — 1차/2차 보완 여부 결정.

2. **자동화 환경 정보 수집** (코드 자동 탐색):
   - 프론트 자동화 도구·디렉토리·fixture 위치
   - 백엔드 자동화 도구·디렉토리·helper 위치
   - 테스트 계정·셋업 명령어·실행 명령어
   - 셀렉터·엔드포인트 추정 패턴
   - **서버 BASE URL**: 운영 서버 주소를 수집하되, 생성 코드에는 반드시 환경 변수 오버라이드 패턴을 사용한다:
     ```javascript
     const BASE = process.env.TEST_BASE_URL || 'https://운영서버주소';
     ```
     로컬 테스트 시 `TEST_BASE_URL=http://localhost:포트 npx playwright test ...` 로 오버라이드. 이 패턴은 프로젝트가 달라도 동일하게 적용한다.
   - `테스트-리스트-3차-환경.md` 에 기록 (사용자 한 번 확인)

3. **변환 대상 결정**:
   - 2차 케이스 중 자동화 = `API` 또는 `UI` 인 것만 변환 대상.
   - `수동` 분류는 skip + `테스트-리스트-3차-수동목록.md` 에 모음 (사람이 따로 검수).

4. `테스트-리스트-3차-체크리스트.md` 생성:
   ```markdown
   <!-- 작성일: YYYY-MM-DD / 기준 2차 산출물 일자: YYYY-MM-DD -->

   | # | 화면 | 기능 | 케이스 ID | 자동화 | 상태 | 코드 파일 |
   |---|------|------|-----------|:------:|:----:|-----------|
   | 1 | 라스트 목록 | 라스트 검색 | A-1 | API | ⬜ | — |
   ```

5. **곧바로 같은 세션에서 3차-B의 첫 배치(최대 3개)까지 처리** (1차-A → 1차-B 첫 배치, 2차-A → 2차-B 첫 배치 패턴과 동일. 안전 가드 통과용).

6. 사용자에게 리포트 후 종료:
   > "3차-A 완료. 변경사항 N건 / 환경 정보 수집됨 / 변환 대상 K건. 첫 배치 M건 변환 완료. 다음 세션부터 Stop 훅이 자동 처리."

**여기서 멈춘다.** (변경사항이 있는 경우만 사용자 확인 지점)

---

#### 3차-B: 케이스 최대 3개 병렬 변환 (매 스킬 호출당 1배치, Stop 훅으로 반복)

메인 에이전트(오케스트레이터)가 서브에이전트를 최대 3개 동시에 띄운다. 각 서브에이전트는 케이스 1개를 독립 컨텍스트에서 코드로 변환하고 파일만 쓴다. 메인은 결과 수집 후 체크리스트를 일괄 업데이트한다.

1. `테스트-리스트-3차-체크리스트.md` 에서 **⬜ Pending 케이스를 최대 3개** 선택한다. 모든 Pending이 소진되면 3차-C로 전환.

2. 선택된 행들의 상태를 모두 🟡 In Progress 로 업데이트.

3. **Agent 툴로 서브에이전트를 동시에 띄운다** (`subagent_type: "general-purpose"`). 한 메시지에 모든 서브에이전트 호출을 포함해 병렬 실행한다.

   각 서브에이전트 프롬프트 — 메인 에이전트가 아래 템플릿의 `{명세}` 자리에 이 파일의 `##### 3차-B 서브에이전트 작업 명세` 섹션 전체를 인라인으로 붙여넣어 구성한다 (파일 경로 참조 없음):
   ```
   아래 명세를 따른다:

   {##### 3차-B 서브에이전트 작업 명세 섹션 내용 전체}

   담당 케이스:
   - 번호: {N}
   - 케이스 ID: {ID}
   - 화면: {화면}
   - 기능: {기능}
   - 자동화 타입: {API/UI}
   - [상황]: {값}
   - [행동]: {값}
   - [기대 결과]: {값}

   프로젝트 루트: {경로}
   코드 저장 경로: {3차-A에서 정한 디렉토리}
   네이밍 컨벤션: {3차-A에서 정한 규칙}
   자동화 환경: {테스트-리스트-3차-환경.md 핵심 내용 요약}
   ```

4. **모든 서브에이전트가 완료되면** 결과를 수집한다.
   - 코드 파일이 생성되었으면 → ✅ Done, 코드 파일 경로 기록
   - 코드 파일이 없으면 (서브에이전트 실패) → ⬜ Pending 으로 **되돌린다**. 다음 배치에서 재처리.

5. 체크리스트 파일의 해당 행들을 일괄 업데이트한다 (✅ Done + 코드 파일 경로).

6. **상태 리포트 한 줄, 사용자 질문 X**:
   > "배치 처리 완료: #{N1}·#{N2}·... ({성공}건 성공/{실패}건 재예약). 남은 Pending: M개. 다음 배치는 Stop 훅이 자동 전환."

7. 즉시 세션 종료. Stop 훅이 다음 배치 자동 처리.

##### 3차-B 서브에이전트 작업 명세

**이 섹션은 서브에이전트 전용이다.** 메인 에이전트가 이 내용을 서브에이전트 프롬프트에 직접 인라인으로 붙여넣는다. 서브에이전트는 이 명세만 따른다.

**작업**: 담당 케이스 1개를 자동화 코드로 변환 + 파일 저장. 완료 후 80단어 이내 리포트.

**단계 1 — 케이스 → 코드 변환**:
- [상황] → setup/fixture 호출 (시드 데이터 셋업)
- [행동] → 자동화 환경의 도구 syntax (API: `request.post(...)` / UI: `page.getByRole(...).click()` 등)
- [결과] → 단언문 (status·body·visible·text 등)
- 전달받은 자동화 환경 요약에서 도구 syntax 참조.

**단계 2 — selector·endpoint 매핑** (코드베이스 직접 탐색):
- UI: 컴포넌트 HTML/TS에서 role·label·text 매칭 → 실제 selector 결정
- API: 라우터·핸들러에서 method·path·payload 키 확인

**단계 3 — 자체 검증**:
- [ ] 케이스 [상황]/[행동]/[결과]가 코드에 모두 반영되었는가
- [ ] selector·endpoint가 실제 코드와 일치하는가
- [ ] 시드 셋업이 누락되지 않았는가
- [ ] 케이스 의도가 syntax 변환 과정에서 왜곡되지 않았는가
- [ ] **차단/거부가 기대 결과인 케이스에서 단언문이 실제로 throw되는가** — `if (blocked) { 기록 } else { 기록 }` 처럼 조건문으로만 분기하고 throw 없이 끝나는 코드는 금지. 테스트 러너는 throw가 없으면 통과로 기록하므로, 차단이 안 돼도 초록불이 된다.

**단계 4 — 코드 파일 저장**: 전달받은 저장 경로·네이밍 컨벤션을 따른다.

**단계 5 — 참조 코드 파일 기록**: 파트 파일(또는 코드 파일 헤더 주석)에 단계 2에서 실제로 읽은 코드 파일 목록을 기록한다. 증분 재실행 시 코드 변경 → 영향 케이스 매핑의 근거로 사용:

```markdown
## 참조 코드 파일
- src/app/pages/home/last/list/last-list.component.ts
- src/app/service/api.service.ts
- lms-server/src/routers/lms.router.ts
```

**완료 리포트** (80단어 이내, 채팅에 출력):
- 케이스 ID/기능명, 코드 파일 경로, 참조 코드 파일 수, 특이사항.
- **사용자에게 질문하지 않는다.** 이 리포트는 메인 에이전트가 수집하는 용도이며 사용자에게 직접 보이지 않는다.

---

#### 3차-C: 실행 + 결과 보고 (모든 Pending 소진 시)

1. **자동화 코드 전체 실행** (3차-A의 실행 명령어 사용).

2. **결과 집계**:
   - 통과/실패/에러 수
   - 실패 케이스별 사유 (timeout / assertion fail / runtime error 등)

3. **실패 분석**:
   - 케이스 표현 문제 (상황/행동/결과 모호) → 케이스 수정 후 재변환 필요
   - 코드 변환 문제 (selector·endpoint 오류) → 자동화 코드 수정 후 재실행
   - 실제 버그 (코드 동작 자체의 문제) → 별도 이슈 보고

4. `테스트-리스트-3차-결과.md` 작성:
   ```markdown
   ## 요약
   - 전체: N건 / 통과: P건 / 실패: F건

   ## 실패 케이스
   | 케이스 ID | 코드 파일 | 실패 사유 | 분류(케이스/코드/버그) |
   ```

5. 파일 열기 규칙에 따라 결과 파일 + 변경점검 파일 + 수동목록을 모두 연다.

6. **스냅샷 저장** — `테스트-리스트-스냅샷.json` 의 `"3차"` 키 추가/업데이트:
   ```json
   "3차": {
     "completed_at": "YYYY-MM-DD",
     "git_commit": "<HEAD commit hash>"
   }
   ```
   코드 기반 증분 감지의 기준점. `git rev-parse HEAD` 로 commit hash 기록.

7. 사용자에게 리포트:
   > "3차 완료. 변환 K건 / 통과 P / 실패 F. 실패 분류: 케이스 X / 코드 Y / 버그 Z. 수동 검수 W건은 별도. 결과 파일부터 보시면 됩니다."

**여기서 멈춘다.** 끝.

---

### 증분 재실행

1차·2차·3차가 완료된 프로젝트에서 코드나 산출물이 바뀌었을 때, 전체 재실행 없이 변경된 부분만 재처리한다.

**진입 조건**: `테스트-리스트-스냅샷.json` 이 존재하고, 사용자가 "증분 업데이트" / "바뀐 것만 다시 해줘" 류로 요청할 때.

#### 증분-1: 변경 감지

1. **문서 변경 감지** — 스냅샷의 `source_hashes` 와 현재 산출물 파일 해시를 비교:
   ```bash
   shasum -a 256 docs/**
   ```
   - **텍스트 파일** (.md, .txt): 해시 변경 → 해당 문서를 `근거` 컬럼에 포함한 1차 파트 파일 grep으로 영향 화면 특정.
   - **바이너리 파일** (.xlsx, .pdf): 해시 변경 → 섹션 단위 감지 불가. 해당 문서를 참조한 **화면 전체** 재처리. 사용자에게 명시: "이 문서는 섹션 감지 불가 → 참조 화면 N개 전체 ⬜ 처리."

2. **코드 변경 감지** — 스냅샷 `git_commit` 기준으로 diff:
   ```bash
   git diff {snapshot_commit}..HEAD --name-only
   ```
   변경된 파일 목록 → 3차 파트 파일의 `## 참조 코드 파일` 섹션과 대조 → 영향 케이스 특정.

#### 증분-2: Cascade ⬜ 처리

| 변경 원인 | 재처리 범위 |
|-----------|------------|
| 문서 변경 | 영향 화면의 1차 파트 ⬜ → 해당 화면 관련 2차 파트 ⬜ → 해당 2차 케이스 관련 3차 케이스 ⬜ |
| 코드만 변경 | 영향받은 3차 케이스만 ⬜ |

체크리스트 행을 ⬜로 되돌리고, 해당 파트 파일을 삭제한다 (재생성 대상임을 명확히).

#### 증분-3: 사용자 확인 후 실행

사용자에게 리포트:
> "변경 감지 완료. 문서 N건 수정 → 화면 M개 영향 / 코드 K파일 수정 → 케이스 J개 영향.
> cascade 포함 ⬜ 예정: 1차 A건 / 2차 B건 / 3차 C건. 진행할까요?"

확인 후 → 체크리스트 ⬜ 처리 → Stop 훅이 기존 루프(1차-B → 2차-B → 3차-B)를 자동 실행.

**스냅샷 업데이트**: 재처리 완료된 스테이지의 스냅샷을 갱신한다 (1차-C / 2차-C / 3차-C 완료 시 각각).

---

## 원칙

### 포함 원칙
- IA에 존재하는 모든 화면에 대한 테스트 케이스가 존재해야 한다.
- 모든 기능에 대해 정상 흐름과 예외 상황을 빠짐없이 작성한다.
- 분리 축이 있는 경우, **축값별 허용/차단 확인 케이스**를 빠짐없이 작성한다. 차단 시에는 UI 숨김 + 경로 차단 + 백엔드 거부 3중 검증.
- 분리 축이 있는 경우, **기능의 심화 시나리오는 대표 축값 1개로만** 작성한다. 역할별로 동일 시나리오를 복제하지 않는다.

### 제외 원칙
- 자명한 케이스를 넣지 않는다. (예: "입력란에 텍스트를 입력하면 정상 입력된다" 등 OS가 보장하는 기본 동작)
- 뒤의 케이스가 앞의 케이스를 포함하면, 뒤의 케이스만 남긴다. (예: "사이트 접속 성공" + "로그인 성공" → 로그인만 남김)
- UI 디테일은 넣지 않는다. 레이아웃 배치, 색상 코드, 애니메이션, 텍스트 포맷, 다국어 표기 형식 등은 기능 테스트 대상이 아니다.
- 정적 텍스트 표시(버전 표시, 안내 문구 등)는 넣지 않는다. 단, 해당 텍스트가 기능 동작의 결과(예: 에러 메시지 → 재시도)인 경우는 기능으로 포함한다.
- 화면 내 네비게이션(뒤로가기, 탭 이동 등)은 독립 항목으로 넣지 않는다. 해당 화면의 기능 테스트 과정에서 자연스럽게 검증된다.

### 기준 작성 원칙
- 각 케이스의 기대 결과는 구체적으로 기술한다. 모호한 표현 금지.
- 다음 형식을 사용한다:
  - [상황]: ...인 상태에서
  - [행동]: ...을 수행하면
  - [결과]: ...이어야 한다
- **차단/거부가 기대 결과인 케이스는 반드시 단언문(expect)이 throw를 발생시켜야 한다.** 차단 여부를 조건문으로 분기해 각 경우를 기록만 하고 throw 없이 끝내면 안 된다 — 테스트 러너는 throw가 없으면 항상 통과(초록불)로 기록하기 때문에, 실제로 차단이 안 돼도 테스트가 성공한 것처럼 보인다.

### 근거 원칙
- 분리 축, 기능 분류, 세부 케이스 모두 기획 산출물에 근거해야 한다.
- 각 케이스에 근거(산출물명 + 해당 위치)를 명시한다.
- 근거를 제시할 수 없는 항목은 포함하지 않는다.
- 단, **산출물에 근거가 없지만 중요해 보이는 항목은 사용자에게 직접 질문하여 확정**한다. 코드에만 있고 문서에 없는 축/기능은 "문서화 필요" 플래그를 붙여 별도 섹션에 둔다.

### 사용자 질문 원칙
- 축 탐색 실패, 근거 없음, 화면 범위 모호 등 **자동으로 결정할 수 없는 것은 반드시 사용자에게 묻는다**. 추측으로 채우지 않는다.
- 질문은 한 번에 묶어서 한다. 각 질문에는 기본값 제안을 포함한다. (예: "포함하시겠어요? (기본: 제외)")

## 사용 예시

- "/generate-test-list"
- "/generate-test-list /Users/seojin/work/Crazyup/프로젝트명"
- "테스트 리스트 만들어줘"
- "회귀 테스트 케이스 작성해줘"
