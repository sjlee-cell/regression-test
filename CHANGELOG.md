# test-automation-builder 스킬 변경 기록

## 2026-05-07

### 기능 단위 원칙 추가 (1차-B 화면 범위 자체 검증)
- **변경 내용**: "기능 1개 = 고객사가 개발자에게 별도로 요청했을 법한 사용자 목적 단위" 원칙 추가
- **계기**: 로그인 화면에 "아이디 입력", "비밀번호 입력", "한글 입력 차단" 등 구현 세부사항이 독립 기능으로 쪼개져 있던 문제
- **판별 질문**: "고객이 기획 미팅에서 '이것도 별도로 넣어달라'고 말했을 것인가?" → 아니오면 상위 기능에 흡수

### 브라우저·OS 기본 UX 흡수 대상 명시
- **변경 내용**: 브라우저/OS가 암묵적으로 제공하는 UX(아이디 저장, 비밀번호 표시 토글 등)는 독립 기능이 아님을 명시
- **계기**: "아이디 저장"이 Chrome 기본 기능임에도 별도 기능으로 등록되어 있던 사례

### 하나의 폼 = 하나의 기능 원칙 추가
- **변경 내용**: 여러 필드를 채워 제출하는 흐름은 필드별로 쪼개지 않고 하나로 통합하는 규칙 추가
- **계기**: 사용자 추가 화면에서 "사용자 구분 선택", "아이디 중복 검증", "사용자 추가"가 3개로 분리되어 있던 문제 → "사용자 추가 (구분·아이디·비밀번호·이름·휴대폰)" 하나로 통합
- **예시**: `user-add.component` 화면의 폼 필드 통합 사례

### 기능의 핵심 결과를 1차 비고에 명시하는 원칙 추가
- **변경 내용**: 기능 실행 후 상태 변화·UI 전환·피드백 등 핵심 결과를 1차 비고에 반드시 명시하도록 자체 검증 체크리스트에 추가
- **계기**: 2차는 1차를 입력으로 사용하며 코드를 재분석하지 않으므로, 1차에 결과가 빠지면 2차 테스트 케이스에서도 검증 포인트가 누락됨
- **예시**: "활성화/비활성화 토글" → 비고에 버튼 레이블 전환(활성↔비활성), 스낵바 메시지 명시

### 입력 형식 조건 별도 기능 등록 규칙 추가
- **변경 내용**: 고객이 명시적으로 요청한 입력 규칙(이메일 형식, 비밀번호 조건, 숫자만 입력 등)은 폼 제출 기능과 분리해 별도 행으로 등록
- **계기**: 사용자 추가 화면에서 아이디·비밀번호·휴대폰 각각에 명시된 형식 조건이 기능 목록에 누락되어 있던 문제
- **적용**: 인증 관련 화면(로그인·비밀번호 재설정/변경·사용자 추가) 공통 적용

### 화면 간 진입 버튼 기준 세분화
- **변경 내용**: 진입 버튼 제외 기준을 "목적지 접근 경로"로 세분화
  - 목적지가 이 버튼에서만 접근 가능 → 제외 (그 화면 테스트로 자연 검증)
  - 목적지가 다른 경로로도 접근 가능 → 포함 (버튼 동작 여부가 별도 검증 필요)
- **계기**: #5 사용자 정보의 "이력 보기" 버튼들이 일괄 제거됐다가 복구됨. 이력 화면은 메뉴에서도 접근 가능하므로 해당 화면 테스트만으로 버튼 동작을 검증할 수 없음
- **예시**: 목록→추가 화면(추가 버튼에서만 진입) → 제외 / 사용자 정보→이력 화면(메뉴에서도 접근) → 포함
